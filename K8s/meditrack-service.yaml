apiVersion: v1
kind: Service
metadata:
  name: meditrack-api-service
spec:
  selector:
    app: meditrack        
  ports:
    - protocol: TCP
      port: 5000         
      targetPort: 5000   
  type: LoadBalancer
